(ns comment-completion-repro.core-test
  (:require [clojure.test :refer :all]
            [comment-completion-repro.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
